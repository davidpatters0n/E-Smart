
<tr>
  <td><%= line_item.quantity %>&times;</td>
  <td><%= line_item.product.title %></td>  <!--Get the product title from doing "line_item > product > title -->
  <td class="item_price"><%= number_to_currency(line_item.total_price, :unit => "&pound;") %></td>
</tr>
